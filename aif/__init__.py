"""Active inference in plain numpy, written to be read rather than deployed.

Every intermediate quantity is returned rather than hidden inside a method, so
a lesson can print and plot the parts of any computation."""
