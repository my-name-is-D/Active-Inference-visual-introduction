"""Placeholder package standing in for aif/ during the deployment spike.

Its only purpose is to prove that a local pure-Python module is bundled into a
wheel and imported successfully inside Pyodide. If this import fails in the
deployed page, the package layout for the whole tutorial has to change.
"""
